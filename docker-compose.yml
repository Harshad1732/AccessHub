services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: accesshub
    ports:
      - "5432:5432"
    volumes:
      - accesshub-pg:/var/lib/postgresql/data

volumes:
  accesshub-pg:
